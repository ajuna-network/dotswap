import NotificationsModal from "../../components/organism/NotificationsModal";

const TestPage = () => {
  return (
    <div>
      Testing
      <NotificationsModal open={true} onClose={() => {}} />
    </div>
  );
};
export default TestPage;
